# ============================================================
# Smart Classroom AI — Python Backend Dependencies
# Generated with: pip freeze > requirements.txt
# Python version: 3.14.x (dev PC) | use 3.10.x on Raspberry Pi
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2

# Flask internals (pinned for reproducibility)
blinker==1.9.0
click==8.3.3
colorama==0.4.6
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# ── Computer Vision ──────────────────────────────────────────
opencv-python==4.13.0.92
numpy==2.4.4

# ── Image Processing ─────────────────────────────────────────
pillow==12.2.0

# ── Configuration ────────────────────────────────────────────
python-dotenv==1.2.2

# ── Face Recognition ─────────────────────────────────────────
# WINDOWS: requires cmake + dlib first:
#   pip install cmake
#   pip install dlib
#   pip install face-recognition
#
# RASPBERRY PI (recommended):
#   sudo apt install -y libatlas-base-dev libhdf5-dev cmake build-essential
#   pip install face-recognition
#
# Uncomment the line below once dlib is installed:
# face-recognition==1.3.0

# ── Raspberry Pi GPIO (RPi ONLY) ─────────────────────────────
# Uncomment on Raspberry Pi when GPIO wiring is ready:
# RPi.GPIO==0.7.1
# gpiozero==2.0.1
